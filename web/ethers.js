// Single place to change how ethers reaches the browser. Swapping the CDN for a
// bundled copy later means editing this file and nothing else.
export * from "https://cdn.jsdelivr.net/npm/ethers@6.17.0/+esm";
